# dialkeep-keepwarm

Pings https://line.dialkeep.ai/health every 5 minutes so the serverless phone
line stays warm. Lives in its own public repo because public-repo GitHub
Actions minutes are free and unlimited; the main repo is private (where this
schedule would bill ~8,700 min/month). Nothing sensitive here — the URL is
public DNS.
